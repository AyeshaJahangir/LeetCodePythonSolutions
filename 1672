class Solution(object):
    def maximumWealth(self, accounts):
        """
        :type accounts: List[List[int]]
        :rtype: int
        """
        for index, elem in enumerate(accounts):
            accounts[index]=sum(elem)
        return max(accounts)
        
