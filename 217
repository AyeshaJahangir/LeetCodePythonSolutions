class Solution(object):
    def containsDuplicate(self, nums):
        """
        :type nums: List[int]
        :rtype: bool
        """
        original_len=len(nums)
        distinct_len=len(set(nums))
        if original_len-distinct_len==0:
            return False
        else:
            return True
        
