class Solution(object):
    def restoreString(self, s, indices):
        """
        :type s: str
        :type indices: List[int]
        :rtype: str
        """
        s= list(s)
        z=zip(indices,s)
        z.sort()
        return "".join([e[1] for e in z])
            
        
